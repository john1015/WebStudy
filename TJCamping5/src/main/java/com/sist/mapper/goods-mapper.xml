<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
 CAMP_NO                                   NOT NULL NUMBER
 CAMP_PRICE                                         NUMBER
 CAMP_NAME                                 NOT NULL VARCHAR2(4000)
 CAMP_ADDR                                          VARCHAR2(4000)
 CAMP_PHONE                                         VARCHAR2(4000)
 CAMP_CONTENT                                       CLOB
 CAMP_IMAGE                                         VARCHAR2(4000)
 CAMP_WIFI                                          VARCHAR2(4000)
 CAMP_STORE                                         VARCHAR2(4000)
 CAMP_ANIMAL                                        VARCHAR2(4000)
-->
<mapper namespace="com.sist.mapper.goods-mapper">
	<select id="goodsListCount" resultType="int">
		SELECT COUNT(*) FROM camp_goods_all
	</select>
	<select id="goodsListData" resultType="GoodsVO" parameterType="hashmap">
		SELECT cno,name,price,poster,num
		FROM (SELECT cno,name,price,poster,rownum as num
		FROM (SELECT cno,name,price,poster
		FROM camp_goods_all ORDER BY cno ASC))
		WHERE num BETWEEN #{start} AND #{end}
	</select>
	<select id="campRowCount" resultType="int">
		SELECT COUNT(*) FROM camp_goods_all
	</select>
<!--	<select id="campDetailData" resultType="CampVO" parameterType="int">
 	SELECT no,name,subject,content,TO_CHAR(regdate,'YYYY-MM-DD') as dbday,hit
		FROM board
		WHERE no=#{no}
	</select> -->
	<select id="campDetailData" resultType="CampVO" parameterType="int">
		SELECT cno,name,brand , delivery , saleprice , price , poster , detail_poster , origin , type
		FROM camp_goods_all
		WHERE cno=#{cno}
	</select>
	<select id="goodsTotalPage" resultType="int">
		SELECT CEIL(COUNT(*)/20.0) FROM camp_goods_all
	</select>
	<!--  맛집 상세보기 -->
<!-- 	<update id="goodsHitIncrement" parameterType="int">
		UPDATE camp_goods_all SET
		hit = hit+1
		WHERE fno = #{fno}
	</update> -->
</mapper>